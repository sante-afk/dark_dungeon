package races;

public class Elf extends Hero {

    public Elf (String name, int health, int armor, String race, int level,
                double exp, int expEnd, int minDamage, int maxDamage) {
        super(name, health, armor, race, level, exp, expEnd, minDamage, maxDamage);
    }

}
