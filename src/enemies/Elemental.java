package enemies;

public class Elemental extends Enemy {

    public Elemental (String name, int health, int armor, String race, int level, int minDamage, int maxDamage) {
        super(name, health, armor, race, level, minDamage, maxDamage);
    }
}
